/**
 * Подробный пример ТЗ для AI-генератора (Canonical AI IR → граф → aiogram 3).
 * Короткий prompt в UI ограничен; этот текст — ориентир для развёрнутых сценариев.
 */
export const CALCULATOR_BOT_DETAILED_PROMPT = `Создай Telegram-бота «Калькулятор».

При /start:
- uiState с сообщением "Привет! Я калькулятор 🔢\\nВыберите действие:"
- reply-кнопки: "+ Сложить", "- Вычесть", "× Умножить", "÷ Разделить"

При нажатии каждой кнопки:
- callback handler с trigger == тексту кнопки
- remember метку операции (сложение/вычитание/умножение/деление)
- run_scenario «два_числа»

Сценарий «два_числа» (steps):
- ask "Введите первое число:" → а
- ask "Введите второе число:" → б
- condition по метке операции: сложить/вычесть/умножить; для деления если б==0 — message "На ноль делить нельзя ❌" и stop
- remember результат
- message "Ответ: {результат}"
- buttons "Ещё раз" → callback возвращает в главное меню
- stop

Верни Canonical AI IR (handlers, scenarios, uiStates), не editor stacks.`;
