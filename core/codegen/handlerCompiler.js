/** Handler tree compilation — re-export from compileCore. */
export {
  compileHandlerTree,
  compileHandlerFromRoot,
  compileStackBody,
  compileConditionSequence,
} from './compileCore.js';
