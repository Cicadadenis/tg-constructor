/** Media block compilers — re-export from compileCore. */
export {
  compilePhoto,
  compileVideo,
  compileAudio,
  compileSticker,
  compileContact,
  compileLocation,
  compilePoll,
  compileDocumentSend,
  compilePhotoVar,
  compileDocumentVar,
  compileSendFile,
  compileMedia,
} from './compileCore.js';
