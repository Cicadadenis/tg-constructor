# basic_handlers

| Example | Key | Highlights |
|---------|-----|------------|
| Echo bot | `echo` | `start`, `command`, `on_text`, reply keyboard on `message` |
| Shop bot | `shop` | `commands`, `global`, catalog/cart commands, `callback` text handlers |

Load: **Примеры** → Echo / Shop.
