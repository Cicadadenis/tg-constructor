# media

| Example | Key | Highlights |
|---------|-----|------------|
| Media demo | `media` | `photo`, `video`, `document`, `on_photo` ack |

Each media block is chained after a handler entry; optional `message` caption before sends.
