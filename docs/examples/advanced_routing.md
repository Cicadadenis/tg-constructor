# advanced_routing

| Example | Key | Highlights |
|---------|-----|------------|
| Routing demo | `full` | `global`, `condition`/`else`, `commands`, inline settings |
| Full Test | `fullTest` | Multi-handler smoke: FSM `ask`, `goto`, media, inline |

Uses handler columns (`COL_Y_STRIDE`) so `flowToStacks` preserves one stack per entry point.
