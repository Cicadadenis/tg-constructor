# fsm

| Example | Key | Highlights |
|---------|-----|------------|
| Profile | `fsm` | `/profile` → `ask` ×2 → `save` → `message` (StatesGroup generated at compile time) |

FSM blocks only appear inside handler stacks, never as graph root.
