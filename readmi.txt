# ✦ Cicada Studio — READMI (legacy .txt)

> Актуальная версия гайда: **[READMI.md](READMI.md)**  
> Ниже — краткая копия для совместимости.

См. полный operational guide в READMI.md:

- Webinstall на http://IP:7700 (Монитор / Установка / Файлы)
- Swap из браузера (sudo python3 webinstall.py)
- Файловый менеджер: копировать, скачать, изменить, загрузить
- API: /api/files/write, /api/files/upload, /api/files/download

Быстрый старт:

```bash
npm install && cp env.example .env && npm run dev
```

Сервер:

```bash
cd /path/to/tg-constructor && git pull && sudo python3 webinstall.py
```

Открыть: http://IP:7700/
