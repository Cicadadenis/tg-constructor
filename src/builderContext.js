import React from 'react';

export const BlockInfoContext = React.createContext(null);
export const AddBlockContext = React.createContext(null);
export const BuilderUiContext = React.createContext(null);
