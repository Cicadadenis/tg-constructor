/**
 * Canvas domain — GraphDocument-native ReactFlow canvas.
 * Rendering path: GraphDocument → canvasProjection → ReactFlow nodes/edges.
 * No stack transforms.
 */

export { ReactFlowCanvas } from '../../builder/ReactFlowCanvas.jsx';
export { GraphCanvas } from '../../builder/GraphCanvas.jsx';
