/**
 * Example graph loader — loads and validates example flows.
 * Direct GraphDocument hydration; no stack projection.
 */

export { EXAMPLE_GRAPH_FLOWS, EXAMPLE_LABELS, EXAMPLE_KEYS } from '../../exampleGraphFlows.js';
export { fitViewportToNodes } from '../hydration/graphHydration.js';
