/**
 * UI-only block metadata — NOT execution runtime.
 * Re-exported from core for builder forms; constructor layer must not import core/runtime.
 */

export {
  HANDLER_ROOT_TYPES,
  OUTPUT_BLOCK_TYPES,
  DELEGATE_BLOCK_TYPES,
  INLINE_BLOCK_HEADERS,
  RUNTIME_PROPERTY_NAMES,
} from '../../core/runtime/rules.js';
