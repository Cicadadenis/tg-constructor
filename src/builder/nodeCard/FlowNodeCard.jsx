/**
 * Canvas node card — delegates to ManyChat-style visual layer.
 * GraphDocument.node.type remains runtime; visual mapping is editor-only.
 */
export { default } from '../visualNodes/VisualFlowNodeCard.jsx';
export { NODE_CARD_WIDTH } from '../visualNodes/VisualFlowNodeCard.jsx';
